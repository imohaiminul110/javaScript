const express = require('express');
const router = express.Router();

// Define category routes
router.post('/', /* Your add category controller */);
router.get('/', /* Your view all categories controller */);

module.exports = router;
